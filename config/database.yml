development:
  adapter: postgresql
  encoding: unicode
  database: store_engine_development
  username: vagrant
  pool: 5 
  template: template0

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# config/database.yml
test:
  adapter: postgresql
  encoding: unicode
  database: store_engine_test
  username: vagrant
  pool: 5
  template: template0

# config/database.yml
production:
  adapter: postgresql
  encoding: unicode
  database: store_engine_production
  username: vagrant
  pool: 5 
  template: template0
